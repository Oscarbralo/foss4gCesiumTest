import {
    Cartesian2,
    VerticalOrigin,
    LabelStyle,
    Color,
    ScreenSpaceEventType,
    ScreenSpaceEventHandler,
    defined
} from "cesium";

export function addlabel(viewer) {

    

}